using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BounceProperties : MonoBehaviour
{
    [SerializeField]
    private float maxBounceHeight;

    public float MaxBounceHeight { get => maxBounceHeight; set => maxBounceHeight = value; }


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
